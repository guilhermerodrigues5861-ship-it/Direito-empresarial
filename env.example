# Opcional: o app já vem com um valor padrão embutido em src/lib/supabase.ts
# (as mesmas credenciais que já estavam no index.html original). Só crie um
# .env.local com estas variáveis se quiser apontar para OUTRO projeto
# Supabase — a chave anon é pública por design, a segurança vem das
# policies de RLS e das funções RPC descritas em supabase/schema.sql.
VITE_SUPABASE_URL=https://wrwtbybnwivrmtrebctp.supabase.co
VITE_SUPABASE_ANON_KEY=cole-aqui-a-chave-anon
